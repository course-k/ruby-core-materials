# なぜこう書くか（rb1-06-strings-symbols）

模範解説（`commentary.md`）を開く**前**に、自分の言葉で書く。判定は「この雛形と差分があること」だけを見る。
中身の合否は付かない。

## 書き写して気づいたこと

<!-- ここに書く -->

## 次の問いに自分の言葉で答える

- `"#{x}"` の補間と `'#{x}'` の違いは何か。
- 1 行目の `# frozen_string_literal: true` は文字列に何をしているか。それが無いと何が変わるか。
- `:name` と `"name"` は何が違うか。Hash のキーにどちらを使ったか。
- `force_encoding` と `encode` はどちらがバイト列を変えるか。

## 分からなかったこと・JS ならこう書くのにと思った箇所

<!-- ここに書く -->
