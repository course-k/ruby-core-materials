# なぜこう書くか（rb1-05-control-flow-io）

模範解説（`commentary.md`）を開く**前**に、自分の言葉で書く。判定は「この雛形と差分があること」だけを見る。
中身の合否は付かない。

## 書き写して気づいたこと

<!-- ここに書く -->

## 次の問いに自分の言葉で答える

- 後置の `if`（`puts x if cond`）は、いつ使うと読みやすいか。
- `and` / `or` と `&&` / `||` は何が違うか。手本でどちらが使われていたか。
- `a&.b` は `a` が `nil` のとき何を返すか。その次の `.c` はどうなるか。
- `exit 1` と `warn` はそれぞれ何を、どこへ出すか。

## 分からなかったこと・JS ならこう書くのにと思った箇所

<!-- ここに書く -->
