# なぜこう書くか（rb1-14-equality-value-objects）

模範解説（`commentary.md`）を開く**前**に、自分の言葉で書く。判定は「この雛形と差分があること」だけを見る。
中身の合否は付かない。

## 書き写して気づいたこと

<!-- ここに書く -->

## 次の問いに自分の言葉で答える

- `==` と `equal?` と `eql?` は、それぞれ何を「同じ」と言っているか。
- `eql?` を自分で書いたら `hash` も書かないといけないのはなぜか。書かないと何が壊れるか。
- `Struct` と `Data` の違いを 1 文で言うと何か。どちらを選ぶ基準を自分はどう置くか。
- `Set` が重複と判定するのに `==` ではなく `eql?` と `hash` を使うのはなぜか。

## 分からなかったこと

<!-- ここに書く -->
