# なぜこう書くか（rb1-13-modules-mixins）

模範解説（`commentary.md`）を開く**前**に、自分の言葉で書く。判定は「この雛形と差分があること」だけを見る。
中身の合否は付かない。

## 書き写して気づいたこと

<!-- ここに書く -->

## 次の問いに自分の言葉で答える

- `module` が 1 つの言葉で「名前空間」と「他のクラスに能力を足す部品」の両方を指すのはなぜか。
- `include` と `extend` は、メソッドが足される先の何が違うか。
- `Comparable` を include すると `<` や `between?` が使えるのはなぜか。自分が書いたメソッドは何か。
- `Enumerable` を include したクラスに `each` を書かないと、何が起きるか。

## 分からなかったこと

<!-- ここに書く -->
