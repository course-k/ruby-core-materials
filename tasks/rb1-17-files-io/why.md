# なぜこう書くか（rb1-17-files-io）

模範解説（`commentary.md`）を開く**前**に、自分の言葉で書く。判定は「この雛形と差分があること」だけを見る。
中身の合否は付かない。

## 書き写して気づいたこと

<!-- ここに書く -->

## 次の問いに自分の言葉で答える

- `File.read` と、ブロック付きの `File.open` は、どちらをいつ使うか。ブロックを付けると何が保証されるか。
- `File.foreach` と `File.readlines` は、同じ結果をどう違うやり方で出しているか。
  大きなファイルではどちらを選ぶか。
- `Pathname` は `File` の各メソッドで書けることを何のために置き換えているか。
- `FileUtils.mkdir_p` が `Dir.mkdir` と違うのはどこか。

## 分からなかったこと

<!-- ここに書く -->
