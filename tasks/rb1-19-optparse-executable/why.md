# なぜこう書くか（rb1-19-optparse-executable）

模範解説（`commentary.md`）を開く**前**に、自分の言葉で書く。判定は「この雛形と差分があること」だけを見る。
中身の合否は付かない。

## 書き写して気づいたこと

<!-- ここに書く -->

## 次の問いに自分の言葉で答える

- shebang の 1 行は誰が読んでいるのか。Ruby は読んでいるか。
- `chmod +x` を忘れると何が起きるか。shebang だけでは足りないのはなぜか。
- `exe/` と `lib/` を分けるのは何のためか。`lib/` に実行のコードを混ぜると何が困るか。
- `OptionParser#parse!` の `!` は何を表しているか。`into:` を使うと何が楽になるか。
- `if __FILE__ == $0` は何と何を比べていて、どんなときに偽になるか。

## 分からなかったこと

<!-- ここに書く -->
