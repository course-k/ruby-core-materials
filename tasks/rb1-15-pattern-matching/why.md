# なぜこう書くか（rb1-15-pattern-matching）

模範解説（`commentary.md`）を開く**前**に、自分の言葉で書く。判定は「この雛形と差分があること」だけを見る。
中身の合否は付かない。

## 書き写して気づいたこと

<!-- ここに書く -->

## 次の問いに自分の言葉で答える

- `case ... when` と `case ... in` は何が違うか。`in` は何を見ているか。
- 配列パターンは全体に一致しないと通らないのに、ハッシュパターンは余分なキーがあっても通る。
  この非対称はどう使い分けに効くか。
- パターンの中に書いた `expectation` と `^expectation` は何が違うか。
- 自作クラスを `in` で分解できるようにするには何を用意すればよいか。
  手本の実行時に出た `deconstruct called` の行は何を教えているか。

## 分からなかったこと

<!-- ここに書く -->
