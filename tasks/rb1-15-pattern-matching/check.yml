# 書き写し課題。judge の構成は docs/check-yml.md の「形式別のひな型」を参照。
task: rb1-15-pattern-matching
title: pattern matching（case / in）
format: 書き写し
submission_dir: rb1-15-pattern-matching

judges:
  - id: copy-check
    type: exercise_test
    name: 写しの照合
    test: copy_test/pattern_matching_test.rb
    load_paths:
      - .
    requires:
      - pattern_matching.rb

  - id: copy-format
    type: rubocop
    name: 写しの書式（RuboCop）
    submission_paths:
      - pattern_matching.rb

  - id: why
    type: why_diff
    name: why.md が雛形と差分あり
    template: why.md
    submission: why.md

  - id: exercise
    type: exercise_test
    name: 確認課題テスト
    test: exercise/test/event_router_test.rb
    load_paths:
      - exercise/lib
    requires:
      - exercise/lib/event_router.rb
